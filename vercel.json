{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "if [ -d \"frontend\" ]; then cd frontend && npm install && npm run build && cp -r dist ../dist; else npm run build; fi",
  "outputDirectory": "dist"
}
